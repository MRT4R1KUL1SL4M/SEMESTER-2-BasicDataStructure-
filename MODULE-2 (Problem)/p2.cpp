#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    vector<int> a(5,n);
    for (int i = 0; i < a.size(); ++i) {
        cout << a[i] <<" ";
        }  
    return 0;
}